<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>리스트 페이지</title>
</head>
<body>
    <div>
        <ul>
            <li class="no">번호</li>
            <li class="title">제목</li>
            <li class="views">조회수</li>
        </ul>
        <% data.forEach((el) => { %>
            <% let link = `/view/${el.no}` %>
            <ul>
                <li class="no"><%= el.no %></li>
                <li class="no"><a href= <%= link %>><%= el.title %></a></li>
                <li class="no"><%= el.views %></li>
            </ul>
        <% }) %>
    </div>
    <a href="/write">글 쓰기</a>
</body>
</html>